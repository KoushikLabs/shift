node_modules/
dist/
.DS_Store
*.local
.vite/

# The specifications are the internal brief this was built from. Their
# "decisions outstanding" sections are written for the commissioner, not the
# public, so they are deliberately not published. Remove these lines to include
# them.
Stakeholder_Tracker_App_SPEC.md
SPEC.md

# Local Supabase credentials for development (the deployed build gets these
# from GitHub Actions variables/secrets instead).
.env
.env.local
